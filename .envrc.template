#!/usr/bin/env bash

# Set UV to use the specific virtual environment
export UV_PROJECT_ENVIRONMENT="$HOME/.venv/memory-database"

# Add the virtual environment to PATH so we can use python directly
PATH_add "$HOME/.venv/memory-database/bin"

# Load project-specific environment variables if they exist
if [[ -f .env ]]; then
    dotenv .env
fi

echo "✓ Using UV environment: $UV_PROJECT_ENVIRONMENT"
